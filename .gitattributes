# Указатели APT читает построчно, и перевод строки в них — LF.
# На Windows git по умолчанию правит его на CRLF при выгрузке; здесь
# это недопустимо: Cydia такой `Packages` разбирает наполовину.
* -text

*.deb binary
*.gz binary
*.bz2 binary
*.png binary
